name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi

      - name: Prepare sample inputs for CI
        run: |
          mkdir -p data/raw
          cp data/raw/sample/*.csv data/raw/

      - name: Build gold (for tests)
        run: python3 src/pipeline/run_all.py

      - name: Run tests
        run: pytest -q
